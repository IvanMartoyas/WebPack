в package.json
    "main": "index.js", я удалил строку  чтобы этим проектом нельзя было
    воспользоваться как npm пакетом
    я закрыл проект указав "private": true
    теперь пакет не будет публековаться npm

    
    webpack-dev-server
    вэб пак хот модуль реплейсмент это когда сервер вэбпака обновляет новые сохраненные данне
    в браузере без перезагрузки, как vue cli.
    Хранит все изменения сервер в ОЗУ.

    "serve": "webpack-dev-server --mode development --open"
    запустит сервер webpack и сразу (--open) откроет окно 